package movies.com.example.movies.models;

public class MovieSubmissionException extends RuntimeException {

    public MovieSubmissionException(String message) {
        super(message);
    }

    // you can add more constructors or methods if needed
}